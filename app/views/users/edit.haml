- provide(:title, "修改信息")
%h1 我的信息
.row
  .span6.offset3
    = form_for(@user) do |f|
      = f.label :name, '姓是名谁'
      = f.text_field :name
      = f.label :email, '家门邮箱'
      = f.text_field :email
      = f.label :password, "开门密钥"
      = f.password_field :password
      = f.label :password_confirmation, "确认密钥"
      = f.password_field :password_confirmation
      = f.submit "保存修改", class: "btn btn-large btn-primary"
    = gravatar_for @user
    %a{:href => "http://gravatar.com/emails"} 改头换面