%a{:href => user_path(current_user)}
  = gravatar_for current_user, size: 78
%h1
  = current_user.name
%span
  = link_to "个人统计", current_user
%span
  共#{current_user.microposts.count}条广播
/ <haml:loud> pluralize(current_user.microposts.count, &amp;quot;micropost&amp;quot;) </haml:loud>