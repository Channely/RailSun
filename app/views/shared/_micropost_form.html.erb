<%= form_for(@micropost) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
    <div class="field">
      <%= f.text_area :content, placeholder: "挥墨于此 . . ." %>
    </div>
    <%= f.submit "播", class: "btn btn-large btn-primary" %>
<% end %>